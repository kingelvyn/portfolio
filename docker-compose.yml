services:
  portfolio-v2:
    image: ghcr.io/kingelvyn/portfolio:v2-03ea1aa
    container_name: portfolio-v2
    ports:
      - "3000:3000"
    restart: unless-stopped
    environment:
      - GEMINI_API_KEY=${GEMINI_API_KEY}